version: 2

models:
  - name: stg_order_items
    description: "Staging model for order items."
    columns:
      - name: order_id
        tests:
          - not_null
      - name: order_item_id
        tests:
          - not_null
      - name: price
        tests:
          - not_null
